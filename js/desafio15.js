function number(n, m){
    let resultado = "";
    if(m > n){
        console.log(resultado = n + " " + "é o número menor")
    }
    if(m < n){
        console.log(resultado = m + " " + "é o número menor")
    }
    if(m > n){
        console.log(resultado = m + " " + "é o número maior")
    }
    if(m < n){
        console.log(resultado = n + " " + "é o número maior")
    }
}